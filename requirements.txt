# ─────────────────────────────────────────────────────────────────────────────
# ARTHA — Indian Markets & Commodities Intelligence System
# ─────────────────────────────────────────────────────────────────────────────

# Core AI Framework
crewai>=0.80.0
crewai-tools>=0.15.0

# LLM Providers & Routing
litellm>=1.89.3
langchain-groq>=0.1.9
langchain-google-genai>=1.0.10
langchain-openai>=0.1.0
langchain-community>=0.2.0

# Web Framework
fastapi>=0.111.0
uvicorn[standard]>=0.30.1
python-multipart>=0.0.9
sse-starlette>=2.1.0

# ── Indian Market Data ─────────────────────────────────────────────────────
jugaad-data>=0.26            # NSE/RBI historical data (primary)
yfinance>=0.2.40             # Fallback for NSE via .NS suffix
httpx>=0.27.0                # HTTP client for API calls

# ── Quantitative Engine ───────────────────────────────────────────────────
numpy>=1.26.0
pandas>=2.2.0
statsmodels>=0.14.0          # Cointegration tests (Engle-Granger, Johansen)
hmmlearn>=0.3.0              # Hidden Markov Model regime detection
scipy>=1.12.0                # Optimization, Student-t distributions
pyarrow>=16.0.0              # Parquet file I/O for data caching
scikit-learn>=1.4.0          # ML: Ridge, RandomForest, HistGBR, Ledoit-Wolf
pykalman>=0.9.7              # Kalman filter for signal smoothing

# ── NLP / Sentiment (Phase 4, but install early to avoid surprises) ───────
# transformers>=4.40.0       # FinBERT — uncomment when Phase 4 starts
# torch>=2.2.0               # PyTorch backend for transformers

# ── Data & Config ─────────────────────────────────────────────────────────
pydantic>=2.7.0
python-dotenv>=1.0.1

# ── Utilities ─────────────────────────────────────────────────────────────
markdown>=3.6
aiofiles>=23.2.1

# ── Optional: OpenAI ─────────────────────────────────────────────────────
openai>=1.30.0

# ── Legacy (kept for backward compat, can be removed after Phase 1) ──────
beautifulsoup4>=4.12.3
lxml>=5.2.0
